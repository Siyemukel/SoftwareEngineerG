import { Outlet } from "react-router-dom";

const Index = () => {
    return (
        <>
            {/* This is the layout landing page magents */}

            <Outlet />
        </>
    );
};

export default Index;
